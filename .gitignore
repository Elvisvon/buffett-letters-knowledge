# macOS
.DS_Store

# 本地 agent 记忆（不公开）
.workbuddy/

# 密钥 / 环境变量（绝不入库；含 API Key 的 .env 只存在于本机）
.env
*.env
!*.env.example

# 个人记录 / 会话草稿（如有，一律不公开）
*.personal.*
*~

# Python
__pycache__/
*.pyc

# 本地运行日志
*.log

# 预览工具产物
.preview/

# macOS App 封装（含本机绝对路径，不入库）
巴菲特投资智慧.app/

# 打包产物（App / DMG）
dist/
*.dmg

# 记忆材料（笔记/收藏/已读/AI 对话）：默认存 ~/Library/Application Support/巴菲特投资智慧/
# （用户级目录，天然不进 Git）；仅当设置 BUFFETT_DATA_DIR 指向项目内时由本规则兜底忽略
.buffett-data/
